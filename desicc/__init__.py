"""Shared library code for the DESI Cosmic Chronometers pipeline (ARTICLE3).

The numbered scripts under pipeline/ are the entry points; everything in
here is plumbing they share so the two variants of a given stage (argparse
vs. interactive) don't drift apart.
"""
